apiVersion: v1
kind: Service
metadata:
  name: {{ include "llama-suite-webui.fullname" . }}
  labels:
    {{- include "llama-suite-webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "llama-suite-webui.selectorLabels" . | nindent 4 }}
